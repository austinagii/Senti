import ChatInput from './ChatInput';

export default {
    component: ChatInput,
    title: "ChatInput",
    parameters: {
        layout: "centered",
    }
}

export const Default = () => <ChatInput />;